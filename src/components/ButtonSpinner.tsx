import React from "react";

const ButtonSpinner = () => {
	return (
		<div
			className={`animate-spin w-[24px] h-[24px] border-[3px] rounded-full border-white/20 !mb-0 border-b-white`}
		/>
	);
};

export default ButtonSpinner;